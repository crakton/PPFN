export const base_url = 'https://www.ppfnhealthapp.com/api';
export const appointment = base_url.concat('/appointment');
export const client = base_url.concat('/beneficiary');
export const provider = base_url.concat('/provider');
export const authentication = base_url.concat('/authenticate');
export const authenticate_client = authentication.concat('/client');
export const authenticate_provider = authentication.concat('/provider');
