import React, {memo} from 'react';
import {SafeAreaView, Text} from 'react-native';

const Training = memo(() => {
  return (
    <SafeAreaView>
      <Text>Training</Text>
    </SafeAreaView>
  );
});

export default Training;
